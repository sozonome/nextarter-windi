import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <div>
      <button className="btn" type="button">
        Hello
      </button>
    </div>
  );
};

export default Home;
